<% content_for(:title, "Quên mật khẩu - boardgame.cf") %>
<div class='ui centered grid sixteen wide column'>
  <div class='row'>
    <h2>Quên mật khẩu</h2>
  </div>
  <div class="row">
    <div class='sixteen wide mobile ten wide tablet nine wide computer nine wide largescreen column'>
      <section class='ui segment'>
        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post , class: 'ui form'}) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <div class="field">
            <%= f.label :email %><br />
            <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
          </div>

          <div class="actions">
            <%= f.submit "Gửi cho tôi email reset mật khẩu" %>
          </div>
        <% end %>
      </section>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>